# Fuse-hit seeds (exceeded max_steps) - reprocessed with extended fuse
# Format: seed_decimal steps_reached
# Fuse-hit seeds (exceeded max_steps) - reprocessed with extended fuse
# Format: seed_decimal steps_reached
